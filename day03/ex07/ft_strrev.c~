#include<stdio.h>
char	*ft_strrev(char *str)
{	
	int len;
	char *temp;
	int x;
	
	temp = str;
	len = 0;
	while (str[len])
	{
		++len;
	}
	++len;
	x = 0;
	while (len >= 0)
	{
		return str;
		--len;
	}
}
int main()
{	char arr[]={"thabang"};
	char*v= ft_strrev(arr);
	printf("%s",v);
}
